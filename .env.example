# ── Required ──────────────────────────────────────────────
OPENAI_API_KEY=sk-your-key-here

# ── Optional ──────────────────────────────────────────────
# OPENAI_MODEL=gpt-4o-mini             # default: gpt-4o-mini
# GITHUB_TOKEN=ghp_your-token          # optional; raises rate limit from 60 → 5,000 req/h
# MAX_CONTEXT_TOKENS=32000             # token budget for the LLM context window
# MAX_FILE_SIZE_KB=200                 # skip files larger than this (KB)
# MAX_FILES_TO_FETCH=30                # cap on individual file downloads per request
# LOG_LEVEL=INFO                       # DEBUG | INFO | WARNING | ERROR
# HOST=0.0.0.0                         # interface the server binds to
# PORT=8000                            # port the server listens on
